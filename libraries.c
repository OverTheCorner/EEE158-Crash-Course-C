// libraries.c
// Demonstrates using standard libraries in C
#include <stdio.h>
#include <math.h>

int main() {
    double x = 16.0;
    double y = sqrt(x);
    printf("Square root of %.2f is %.2f\n", x, y);
    // Exercise: Use math.h to calculate the power of a number
    return 0;
}
